/.*
!/.gitignore
!/.ignore
!/.specs
!/.github
/*.md
!/README.*
!/TODO.*
!/CHANGELOG.*

# Framework symlinks laid down by `nix run .#init` — no trailing slash: a
# symlink is not a directory for git, and `just install` runs `git add .`.
/dnf
/doc/
/src/
/result/
/target/
tmp/
