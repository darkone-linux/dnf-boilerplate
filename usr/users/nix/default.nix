# Home manager settings of the `nix` maintenance user.
#
# `nix` (uid 65000) is declared implicitly by the generator on every host: it
# is the account colmena deploys with. This file must exist — keep it, even
# empty.

{ }
